# SkillTree Codex Plugin

SkillTree is a privacy-first local foundation for routing user-managed Codex Skills.

This P0.1 Bundle contains only offline, hash-verified base artifacts. Installation,
SQLite initialization, and runtime activation are delivered by P0.2.
